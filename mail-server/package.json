{
  "name": "mail-server",
  "version": "1.0.0",
  "description": "Simple mail server for proyecto-ionic (SendGrid or SMTP fallback)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "nodemailer": "^6.9.3"
  },
  "optionalDependencies": {
    "@sendgrid/mail": "^7.7.0"
  }
}
